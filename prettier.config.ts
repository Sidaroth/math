import { type Config } from 'prettier';

const config: Config = {
    tabWidth: 4,
    semi: true,
    singleQuote: true,
    trailingComma: 'all',
    endOfLine: 'lf',
    arrowParens: 'always',
    quoteProps: 'consistent',
};

export default config;
